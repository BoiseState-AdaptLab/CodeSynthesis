# Fake files
fake_nnz_*

# Nested build directory
/build*

# CLion project configuration
/.idea

# Taco benchmarks can be explicitly added with the -f flag, otherwise ignored (they are too large)
test/data/taco_benchmarks/*.mtx
